import { render } from "../ui.js";

export default async function () {
    render(`
        <h2>Analytics</h2>
        <div class="card">
            <p>Charts and graphs coming soon.</p>
        </div>
    `);
}
